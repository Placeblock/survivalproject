const express = require('express');
const app = new express();
var path = require('path')

app.use(express.static(path.join(__dirname, 'public')));

app.listen(80 || 3000, ()=>{console.log("server is up")} )

